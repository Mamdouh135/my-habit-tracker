# Habit Tracker SaaS Frontend

This is the frontend for a multi-tenant (SaaS) habit tracker application. Built with React.

## Features
- User registration and login (to be implemented)
- Habit tracking UI (to be implemented)

## Getting Started
1. Install dependencies: `npm install`
2. Start the app: `npm start`

The app will connect to the backend API for authentication and habit management.
